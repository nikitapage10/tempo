"use client";

import { EmptyShaderPanel } from "@/components/shader-empty";

export default function SceneManageModerationPage() {
  return (
    <EmptyShaderPanel
      title="Moderation is on the way"
      copy="Removed posts, member reports, and escalation to TEMPO Support land with the Scenes feed and moderation package."
    />
  );
}
