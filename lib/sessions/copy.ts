/** Copy shared by the guest gate UI and the server-only link helpers. */
export const SESSION_GUEST_UNAVAILABLE_MESSAGE =
  "This Session link isn’t available. It may have expired, been revoked, or the passcode is wrong.";
